rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The architecture exposes Firestore only through the backend.
    // Cloud Functions uses the Admin SDK, which bypasses these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
